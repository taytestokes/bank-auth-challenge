/*
*
* Create the SQL statements and Logic to have the controllers do what they're supposed to
*
*/

const checkBalance = async (req, res) => {
    // retunr savings and checking balance
};

const depositSavings = async (req, res) => {
    // deposit money into savings account
};

const withdrawSavings = async (req, res) => {
    // withdraw from savings acc
};

const depositChecking = async (req, res) => {
    // deposit into checking
};

const withdrawChecking = async (req, res) => {
    // withdraw from checking acc
};

// Black Diamond
const transfer = async (req, res) => {
    // transfer money between accounts
};

module.exports = {
    checkBalance,
    depositSavings,
    withdrawSavings,
    depositChecking,
    withdrawChecking,
    transfer
};